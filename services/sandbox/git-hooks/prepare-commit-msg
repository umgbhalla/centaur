#!/usr/bin/env sh
set -eu

message_file="${1:?missing commit message file}"
trailer="Co-authored-by: Centaur AI <ai@centaur.local>"

if grep -Fqi "$trailer" "$message_file"; then
    exit 0
fi

printf '\n%s\n' "$trailer" >> "$message_file"
